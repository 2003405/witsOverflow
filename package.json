{ 
    "name": "test-project",
    "scripts": {
      "test:coverage": "jest --coverage",
      "test:coveralls": "jest --coverage && coveralls < coverage/lcov.info"
      },
      "install": "npm install coveralls --save-dev"
  }
  
  